# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
.env
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# MLflow runtime state — regenerated by the pipeline at container build time.
mlruns/
mlartifacts/
mlflow.db

# Data — only the raw dataset stays in git; processed splits are derived.
data/processed/

# OS junk.
.DS_Store
Thumbs.db

# Microsoft Office lock files (created while a .docx/.pptx/.xlsx is open).
~$*

# Editors.
.vscode/
.idea/
*.swp
